{% extends 'base.html.twig' %}
    {% block navigation %}
		{% include 'AssuranceFrontBundle:default:navigation.html.twig' %}
    {% endblock %}
{% block slider %}
{% endblock %}
{% block from %}
<div class="row">
		  <div class="col-sm-3">
			<div class="sidebar-nav">
			  <div class="navbar navbar-default" role="navigation">
				<div class="navbar-header">
				  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-navbar-collapse">
					<span class="sr-only">Toggle navigation</span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
					<span class="icon-bar"></span>
				  </button>
				  <span class="visible-xs navbar-brand">Sidebar menu</span>
				</div>
				<div class="navbar-collapse collapse sidebar-navbar-collapse">
				  <ul class="nav navbar-nav">
					<li class="active"><a href="{{ path('fos_user_profile_show') }}">Coordonnees Personnel</a></li>
					<li><a href="{{ path('fos_user_profile_edit') }}"> Editer Profile</a></li>
					<li><a <a href="{{ path('factures') }}">Votre factures</a></li>
				  </ul>
				</div><!--/.nav-collapse -->
			  </div>
			</div>
		  </div>
		
<div class="col-sm-3">
	<ul>
		<li><label style="color:White; font-size:120%;">{{ 'profile.show.username'|trans({}, 'UserBundle') }}:   {{ user.username }}</label></li>
	   <li><label style="color:White; font-size:120%;">{{ 'profile.show.email'|trans({}, 'UserBundle') }}:   {{ user.email }}</label></li>
		<li><label style="color:White; font-size:120%;">{{ 'form.nom'|trans({}, 'UserBundle') }}:   {{ user.firstname }}</label></li>
		<li><label style="color:White; font-size:120%;">{{ 'form.prenom'|trans({}, 'UserBundle') }}:   {{ user.lastname }}</label></li>
	<ul>
</div>
 </div>
{% endblock %}